[Format]
name = MAF
extension = maf
description = Mutation Annotation Format (MAF) is a tab-delimited text file with aggregated mutation information from VCF Files and are generated on a project-level.

[Body]
sep = \t

[Ignore]
regex = ^#
