[Format]
name = CSV
extension = csv
description = In computing, a comma-separated values (CSV) file is a delimited text file that uses a comma to separate values.

[Body]
sep = ,

[Ignore]
regex = ^#
